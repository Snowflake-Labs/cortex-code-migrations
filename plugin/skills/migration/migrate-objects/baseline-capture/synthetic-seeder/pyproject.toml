[project]
name = "synthetic-seeder"
version = "0.1.0"
description = "Isolation metadata enrichment for generated test-case YAMLs"
requires-python = ">=3.11"
dependencies = ["pyyaml>=6.0"]

[dependency-groups]
dev = ["pytest>=9.0.3"]

[tool.uv]
package = false
