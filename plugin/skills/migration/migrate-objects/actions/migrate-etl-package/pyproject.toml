[project]
name = "migrate-etl-package"
version = "1.0.0"
description = "ETL orchestration fixer scripts for SnowConvert AI migrations"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["scripts"]
