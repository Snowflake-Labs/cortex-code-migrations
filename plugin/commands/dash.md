---
description: Opens dashboard in the default browser
allowed-tools: Bash(open:*)
---

If the dashboard port is configured in `configure()`, open it

!`open http://127.0.0.1:<port>/`

Otherwise, load `setup/SKILL.md` to setup the migrations dashboard.
